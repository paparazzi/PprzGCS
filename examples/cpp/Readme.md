# C++ gRPC example

## prerequisites

- Install [gRPC](https://grpc.io/docs/languages/cpp/quickstart/)
- Install PprzGCS with `cmake --install`

## Build

- `mkdir build && cd build`
- Configure with `cmake .. -D CMAKE_PREFIX_PATH="<path/to/pprzgcs>;<path/to/grpc>"`
- `make`


